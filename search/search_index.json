{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"agentseek","text":"<p>agentseek is a database-native Agent Harness. It packages Bub with database-backed runtime storage and a small set of defaults for teams that want agent runtime data to be queryable from the beginning.</p> <p>This overview explains what agentseek is and how the documentation is organized. If you want to run it first, start with Getting started.</p>"},{"location":"#what-it-is","title":"What It Is","text":"<p>agentseek treats runtime data as database data: context, tasks, tool calls, traces, feedback, and conversation history should live in a durable substrate rather than scattered files and side-channel logs.</p> <p>It remains database-neutral. SQLite is enough for local use, and any suitable SQLAlchemy backend can be used. For a smooth local-to-cloud path, we recommend OceanBase seekdb and OceanBase.</p>"},{"location":"#what-it-is-not","title":"What It Is Not","text":"<p>agentseek is not a replacement for Bub. It follows Bub's runtime and extension model, and <code>bub</code> remains available when you want to use the upstream CLI or extension namespace directly.</p> <p>agentseek is also not a single-purpose chat bot. The project boundary is the harness: defaults, packaging, runtime storage, environment aliases, and a small branded entry point.</p>"},{"location":"#documentation-map","title":"Documentation Map","text":"<ul> <li>Getting started: a short tutorial that gets agentseek running locally.</li> <li>Configuration: a reference for environment variables, storage, channels, and onboarding.</li> <li>Extensions: a guide to project instructions, plugins, and skills.</li> </ul>"},{"location":"configuration/","title":"Configuration","text":"<p>This is a reference for agentseek configuration. For a guided setup, use Getting started.</p>"},{"location":"configuration/#environment-naming","title":"Environment Naming","text":"<p>agentseek accepts both <code>AGENTSEEK_*</code> and <code>BUB_*</code>.</p> <p>Prefer <code>AGENTSEEK_*</code> in agentseek projects. At startup, agentseek maps missing <code>BUB_*</code> values from matching <code>AGENTSEEK_*</code> values. If both are set, <code>BUB_*</code> takes precedence.</p>"},{"location":"configuration/#core-variables","title":"Core Variables","text":"Variable Purpose <code>AGENTSEEK_MODEL</code> Model identifier, such as <code>openrouter:free</code>. <code>AGENTSEEK_API_KEY</code> API key for the configured model provider. <code>AGENTSEEK_API_BASE</code> OpenAI-compatible API base URL. <code>AGENTSEEK_TAPESTORE_SQLALCHEMY_URL</code> SQLAlchemy URL for runtime tape storage. <code>AGENTSEEK_HOME</code> Runtime home. Defaults to <code>.agentseek</code> in the current workspace. <code>AGENTSEEK_PROJECT</code> Directory used by <code>agentseek install</code> as Bub's plugin sandbox. Defaults to <code>{AGENTSEEK_HOME}/agentseek-project</code>. Maps to <code>BUB_PROJECT</code>."},{"location":"configuration/#optional-runtime-variables","title":"Optional Runtime Variables","text":"Variable Purpose <code>AGENTSEEK_MAX_STEPS</code> Maximum model/tool loop steps. <code>AGENTSEEK_MAX_TOKENS</code> Response token budget. <code>AGENTSEEK_MODEL_TIMEOUT_SECONDS</code> Model request timeout."},{"location":"configuration/#storage","title":"Storage","text":"<p>agentseek uses SQLAlchemy-backed tape storage through <code>bub-tapestore-sqlalchemy</code>.</p> <p>For local development:</p> <pre><code>AGENTSEEK_TAPESTORE_SQLALCHEMY_URL=sqlite+pysqlite:///./agentseek-tapes.db\n</code></pre> <p>For deployment, use any suitable SQLAlchemy URL. OceanBase seekdb and OceanBase are recommended for a good local-to-cloud experience, but they are not required.</p>"},{"location":"configuration/#channels","title":"Channels","text":"<p>Telegram is available through Bub. Feishu is bundled with agentseek.</p> Variable Purpose <code>AGENTSEEK_TELEGRAM_TOKEN</code> Telegram bot token. <code>AGENTSEEK_TELEGRAM_ALLOW_USERS</code> Optional Telegram user allowlist. <code>AGENTSEEK_TELEGRAM_ALLOW_CHATS</code> Optional Telegram chat allowlist. <code>AGENTSEEK_FEISHU_APP_ID</code> Feishu app ID. <code>AGENTSEEK_FEISHU_APP_SECRET</code> Feishu app secret. <code>AGENTSEEK_FEISHU_VERIFICATION_TOKEN</code> Optional Feishu verification token. <code>AGENTSEEK_FEISHU_ENCRYPT_KEY</code> Optional Feishu encrypt key. <p>Other channels can be added through Bub-compatible plugins.</p>"},{"location":"configuration/#onboarding","title":"Onboarding","text":"<pre><code>uv run agentseek onboard\n</code></pre> <p>The command writes configuration under the active agentseek home, which defaults to <code>.agentseek/config.yml</code> in the current workspace.</p>"},{"location":"extensions/","title":"Extensions","text":"<p>This guide explains the supported extension points for agentseek projects. For runtime settings, see Configuration.</p> <p>agentseek follows Bub's extension model. The agentseek layer adds naming conventions, environment aliases, packaging defaults, and bundled skills; it does not replace Bub hooks or entry points.</p>"},{"location":"extensions/#project-instructions","title":"Project Instructions","text":"<p>Use <code>AGENTS.md</code> to teach agents how to operate in your project.</p> <p>Good project instructions usually include:</p> <ul> <li>which channels are enabled, such as <code>$feishu</code> or <code>$telegram</code></li> <li>when the agent should reply directly and when it should call a channel-specific send tool</li> <li>repository-specific coding, testing, and documentation rules</li> <li>runtime constraints that should be followed on every task</li> </ul> <p>Keep <code>AGENTS.md</code> focused on durable behavior. Do not put credentials, deployment-only secrets, or one-off task notes in it.</p>"},{"location":"extensions/#extend-with-plugins","title":"Extend With Plugins","text":"<p>Plugins add runtime behavior through Bub's hook system. Use this path when you need a new channel, model provider, store, tool package, scheduler, or other runtime integration.</p>"},{"location":"extensions/#install-a-plugin","title":"Install A Plugin","text":"<p>Plugins install into the same Python environment as agentseek. Browse the ecosystem on Bub Hub: it lists plugins with install specs such as <code>bub install bub-feishu@main</code>.</p>"},{"location":"extensions/#from-bub-hub","title":"From Bub Hub","text":"<p>Hub entries typically use <code>bub install \u2026</code>. The agentseek CLI exposes the same resolver:</p> <pre><code>agentseek install bub-feishu@main\n</code></pre> <p><code>install</code> accepts package specs described in its help output: a git URL, <code>owner/repo</code>, or a package name published through the Bub contrib resolver (often <code>name@branch</code>). It is not a generic PyPI installer for arbitrary distribution names.</p> <p>By default, agentseek sets <code>BUB_PROJECT</code> to <code>{BUB_HOME}/agentseek-project</code> (so <code>AGENTSEEK_PROJECT</code> can override it). New sandboxes use <code>uv init --name agentseek-project</code> instead of <code>bub-project</code>.</p> <p>Use <code>bub install \u2026</code> instead if you prefer the upstream Bub entry point; behavior matches the Hub examples.</p>"},{"location":"extensions/#path-or-git-for-agentseek-contrib-packages","title":"Path or Git For agentseek Contrib Packages","text":"<p>Packages that live only in this repository (for example <code>agentseek-schedule-sqlalchemy</code> under <code>contrib/</code>) are not guaranteed to resolve when you run <code>agentseek install agentseek-schedule-sqlalchemy</code> or <code>uv add agentseek-schedule-sqlalchemy</code> outside the monorepo. Prefer wiring them explicitly:</p> <pre><code>uv add ./contrib/agentseek-schedule-sqlalchemy\n</code></pre> <p>or install from Git with the subdirectory that contains <code>pyproject.toml</code>:</p> <pre><code>uv pip install \"git+https://github.com/ob-labs/agentseek.git#subdirectory=contrib/agentseek-schedule-sqlalchemy\"\n</code></pre> <p>When you vendor the package next to your project, the dependency entry looks like:</p> <pre><code>[project]\ndependencies = [\n    \"agentseek-schedule-sqlalchemy\",\n]\n</code></pre> <p>For workspace packages, also wire the source:</p> <pre><code>[tool.uv.sources]\nagentseek-schedule-sqlalchemy = { workspace = true }\n\n[tool.uv.workspace]\nmembers = [\n    \"contrib/agentseek-schedule-sqlalchemy\",\n]\n</code></pre> <p>The package must expose a Bub entry point:</p> <pre><code>[project.entry-points.bub]\nschedule = \"agentseek_schedule_sqlalchemy.plugin:main\"\n</code></pre> <p>After changing dependencies, refresh the environment:</p> <pre><code>uv lock\nuv sync\n</code></pre>"},{"location":"extensions/#create-a-plugin","title":"Create A Plugin","text":"<p>Create an agentseek-owned plugin under <code>contrib/agentseek-&lt;feature&gt;/</code> unless the user asks for another location.</p> <p>Use these conventions:</p> <ul> <li>distribution name: <code>agentseek-&lt;feature&gt;</code></li> <li>Python package: <code>agentseek_&lt;feature&gt;</code></li> <li>entry point group: <code>[project.entry-points.bub]</code></li> <li>environment variables: prefer <code>AGENTSEEK_*</code>, accept <code>BUB_*</code> when the setting maps to Bub runtime behavior</li> </ul> <p>When both prefixes are supported for the same setting, <code>BUB_*</code> should take precedence. This keeps the plugin usable from plain Bub while letting agentseek projects document <code>AGENTSEEK_*</code> names.</p> <p>agentseek bundles an agentseek-adapted <code>plugin-creator</code> skill to help scaffold or update Bub-compatible plugin packages according to these conventions. It follows the upstream Bub contrib workflow shape, but specializes the guidance for <code>contrib/agentseek-*</code>, bundled <code>src/skills</code>, and <code>AGENTSEEK_*</code> alias behavior in this repository.</p>"},{"location":"extensions/#extend-with-skills","title":"Extend With Skills","text":"<p>Skills teach agents task-specific behavior. Use this path when the extension is instruction, workflow knowledge, or a small script the agent should know how to call. Use a plugin instead when the runtime itself needs a new hook, channel, store, or tool registration.</p>"},{"location":"extensions/#install-skills-in-a-project","title":"Install Skills In A Project","text":"<p>Install project-local skills under:</p> <pre><code>.agents/skills/&lt;skill-name&gt;/SKILL.md\n</code></pre> <p>Install skills from a registry into <code>.agents/skills</code> using <code>npx skills add</code>, matching Bub Hub examples:</p> <pre><code>npx skills add psiace/skills --skill friendly-python\nnpx skills add bubbuild/bub-contrib --skill plugin-creator\n</code></pre> <p>This is the right place for repository-specific workflows that should not ship with the agentseek package.</p>"},{"location":"extensions/#bundle-skills-with-agentseek","title":"Bundle Skills With agentseek","text":"<p>Bundle release skills under:</p> <pre><code>src/skills/&lt;skill-name&gt;/SKILL.md\n</code></pre> <p>Bundled skills are included in the agentseek package because <code>src/skills</code> is part of the build includes.</p> <p>Use bundled skills for behavior that should be available wherever agentseek is installed. Keep them stable, general, and aligned with the agentseek extension model.</p>"},{"location":"extensions/#use-external-skill-sources","title":"Use External Skill Sources","text":"<p>The build can also import selected skills from external repositories through <code>[tool.pdm.build].skills</code>.</p> <p>Use this for shared upstream skills. Prefer bundling under <code>src/skills</code> when the skill is agentseek-specific or has been adapted to agentseek conventions.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This tutorial gets agentseek running locally with SQLite-backed runtime storage. It assumes Python 3.12+ and <code>uv</code> are already available.</p>"},{"location":"getting-started/#1-install","title":"1. Install","text":"<pre><code>git clone https://github.com/ob-labs/agentseek.git\ncd agentseek\nuv sync\nuv run agentseek --help\n</code></pre>"},{"location":"getting-started/#2-configure","title":"2. Configure","text":"<p>Use <code>AGENTSEEK_*</code> variables for the agentseek distribution. They are passed through to Bub as <code>BUB_*</code> aliases.</p> <pre><code>export AGENTSEEK_MODEL=openrouter:free\nexport AGENTSEEK_API_KEY=sk-or-v1-your-key\nexport AGENTSEEK_API_BASE=https://openrouter.ai/api/v1\nexport AGENTSEEK_TAPESTORE_SQLALCHEMY_URL=sqlite+pysqlite:///./agentseek-tapes.db\n</code></pre> <p>The SQLite file is created automatically on first use.</p>"},{"location":"getting-started/#3-run","title":"3. Run","text":"<pre><code>uv run agentseek chat\n</code></pre> <p>You should see an interactive chat session. The example API key is a placeholder; use a real key before expecting model responses.</p>"},{"location":"getting-started/#4-check-local-state","title":"4. Check Local State","text":"<p>agentseek stores local config and runtime state under <code>.agentseek</code> in the current workspace by default.</p> <p>Set <code>AGENTSEEK_HOME</code> or <code>BUB_HOME</code> to use another location.</p>"},{"location":"getting-started/#next","title":"Next","text":"<ul> <li>Run <code>uv run agentseek onboard</code> to collect configuration interactively.</li> <li>Use <code>uv run bub ...</code> when you want the upstream Bub CLI directly.</li> <li>See Configuration for environment variables, storage, and channel settings.</li> </ul>"}]}