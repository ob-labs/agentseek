---
title: <Numbered, beginner-friendly title, e.g. "01 — Run your first chat with agentseek">
type: tutorial
audience: [A1]              # update
runs: yes
verified_on: YYYY-MM-DD
sources:
  - src/agentseek/cli.py
  - pyproject.toml
---

# <Same title without numeric prefix>

> **You will:** <one sentence describing the end state>.
> **You need:** <prereqs as a comma-separated list>.
> **Time:** <not required — omit unless very short>.

This is a learning-oriented walk-through. Every step is executed in order. Do not skip steps;
if something fails, fix it before moving on rather than working around it — the next steps
assume the previous one succeeded.

## 1. <First milestone — what they get after this step>

<Short paragraph: what we are about to do and why.>

```bash
<copy-pasteable command>
```

```text title="expected output"
<the *actual* output you saw, trimmed to the parts that matter>
```

<One-sentence checkpoint: "You should now see …">

## 2. <Second milestone>

…

## What you have now

- <bullet — concrete artefact, e.g. ".agentseek/config.yml on disk">
- <bullet>

## Where to go next

- For the same outcome through your own app, see `../tutorials/02-first-harness-app.md`.
- To look up exact variable names, see `../reference/environment.md`.
- To understand *why* the layout is project-local, see `../explanation/what-agentseek-is.md`.
