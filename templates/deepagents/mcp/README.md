# DeepAgents — mcp template

Scaffolds a DeepAgents application that discovers prefixed MCP Tools from
validated `.mcp.json` stdio or HTTP connections. The generated project includes
a local calculator server, model-free MCP smoke task, streamed React UI, and
AgentSeek lifecycle spec.
